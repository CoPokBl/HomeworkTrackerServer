using System.Drawing;

namespace HomeworkTrackerServer {
    public class ColouredString {
        public string Text;
        public Color Color;

        public ColouredString(string text, Color color) {
            Text = text;
            Color = color;
        }
        
    }
}